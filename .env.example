# Leave unset when /api/soundcloud/resolve is routed to the Worker on the same origin.
# For a standalone workers.dev deployment, use its complete resolver endpoint instead.
VITE_MEDIA_RESOLVER_URL=https://clyvora-link-resolver.levi-05e.workers.dev/v1/soundcloud/resolve
